import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello from davecal")
            .font(.system(size: 36, weight: .bold))
            .frame(minWidth: 600, minHeight: 400)
    }
}
